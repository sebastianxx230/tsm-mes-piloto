services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: tsm-mes-piloto:local
    env_file:
      - .env.container
    ports:
      - "8000:8000"
    init: true
    restart: unless-stopped
    stop_grace_period: 70s
